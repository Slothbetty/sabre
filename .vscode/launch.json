{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug SABRE",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/sabre.py",  // Updated path to include src folder
            "args": [
                "-v",
                "-a", "bola",
                // "--seek", "30", "120",
                "--movie", "movie.json",
                "-sc", "seeks.json"
            ],
            "cwd": "${workspaceFolder}/src",  // Set working directory
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}